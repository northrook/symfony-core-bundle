<!DOCTYPE html>
<html lang="{$get->language}" id="top" theme="{$get->theme}" status="init">
<head>

    <meta charset="UTF-8">
    <title>{$get->document->title}</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">

    {*    <script n:foreach="$document->scripts as $path" src="{$path->value}" defer></script>*}

    {*    {$get->coreScripts()}*}
</head>

{dump( $get, $document )}}
{*{$get->}*}
{*{$get->sitename ?? null | echo}*}
{*<!-- {$document->echo('open')} -->*}
{block 'body'}{/block}
{*<!-- {$document->echo('close')} -->*}
</html>